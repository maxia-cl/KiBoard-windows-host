//! mDNS advertisement (`_kiboard._tcp.local.`, protocol/README.md §1) lands in **F1** — harvested
//! from the `nervous-swirles-1ed28b` worktree per docs/implementation-plan.md. v1 has no discovery
//! at all (pairing is 100% QR-driven); this module exists now only so the target file layout from
//! the plan is in place before F1 fills it in.
