{
  "name": "kiboard-editor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate-tokens": "node KiBoard-protocol/protocol/generate-tokens.mjs node_modules/.tokens.g.dart src/tokens.g.css",
    "predev": "npm run generate-tokens",
    "dev": "vite",
    "prebuild": "npm run generate-tokens",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@tauri-apps/cli": "^2.11.4",
    "svelte": "^5.56.4",
    "vite": "^8.1.1"
  }
}
